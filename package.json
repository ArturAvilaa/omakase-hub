{
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix client",
    "dev": "npm run dev --prefix client -- --port 8080",
    "build": "npm run build --prefix client",
    "build:dev": "npm run build:dev --prefix client",
    "test": "npm run test --prefix client"
  }
}
