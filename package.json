{
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "bun --cwd client run dev",
    "build": "bun --cwd client run build",
    "build:dev": "bun --cwd client run build:dev",
    "test": "bun --cwd client run test"
  }
}
