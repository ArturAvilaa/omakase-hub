{
  "private": true,
  "scripts": {
    "dev": "cd client && bun run dev",
    "build": "cd client && bun run build",
    "build:dev": "cd client && bun run build:dev",
    "test": "cd client && bun run test"
  },
  "dependencies": {
    "@lovable.dev/cloud-auth-js": "^1.0.1",
    "@supabase/supabase-js": "^2.101.1"
  }
}
