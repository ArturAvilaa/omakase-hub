{
  "private": true,
  "scripts": {
    "dev": "cd client && npx --no-install vite",
    "build": "cd client && npx --no-install vite build",
    "build:dev": "cd client && npx --no-install vite build --mode development",
    "test": "cd client && npx --no-install vitest run"
  }
}
